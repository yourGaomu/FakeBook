<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.zhangzc</groupId>
    <artifactId>bookFramework</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>bookFramework</name>
    <description>bookFramework</description>
    <properties>
        <java.version>17</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <spring-boot.version>3.0.2</spring-boot.version>
    </properties>


    <modules>
        <!-- 通用工具模块 -->
        <module>book-common</module>
        <!-- 接口日志组件 -->
        <module>json-spring-boot-start-jackon</module>
        <module>fakebook-spring-boot-start-biz-operationlog</module>
        <module>fakebook-spring-boot-start-jackon</module>
        <module>fakebook-spring-boot-start-biz-operationlog/fakebook-spring-boot-start-context</module>
        <module>kafka-spring-boot-start</module>
        <module>leaf-spring-boot-starter</module>
        <module>minio-spring-boot-start</module>
        <module>redis-spring-boot-start</module>
        <module>sensitiveword-spring-boot-start</module>
        <module>thredlocalpool-spring-boot-start</module>
        <module>mongodb-spring-boot-start</module>
    </modules>

    <!-- 指定父项�?-->
    <parent>
        <groupId>com.zhangzc</groupId>
        <artifactId>fakebook</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <!-- 多模块项目需要配置打包方式为 pom -->
    <packaging>pom</packaging>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>

